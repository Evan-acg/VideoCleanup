# 项目Agents

**回复必须使用中文**

## 关于GIT

1. 当用户要求提交git的时候，请检查项目目录下，是否存在.lazygit.yaml文件，如果存在，使用其中的内容作为构建type(scope): message的内容，如果不存在，请参考此风格来生成， 生成的message应该使用中文，type和scope保持英文。

## 关于修改

1. 每次对代码进行改动时，提交commit消息，如果有必要，分批提交。请注意，在正式提交前，应该询问用户对生成的消息是否认可。
